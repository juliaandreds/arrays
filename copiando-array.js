const notas = [7, 7, 8, 9];

//[...] --> espalhamento, ou seja, ele mantém o original como ele é e em notas notas acrescenta o que foi necessário.
const novasNotas = [...notas, 10];


console.log (`As novas notas são: ${novasNotas}`);
console.log (`As notas originais são: ${notas}`);
